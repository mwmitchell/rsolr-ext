=RSolrParams
A very simple Ruby module for Solr param mapping.

=Usage
  #standard
  mapped_params = RSolr::Params.standard(
    :q=>'one',
    :filters=>{:name=>'one'},
    :phrase_queries=>{:name=>'matt'},
    :phrase_queries=>{:glad=>1}
  )
  
  # dismax
  mapped_params = RSolr::Params.dismax(
    :q=>'one',
    :filters=>{:name=>'one'},
    :phrase_queries=>{:name=>'matt'},
    :boost_queries=>{:cat=>'electronics^5.0'}
  )